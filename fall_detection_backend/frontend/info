
steps followed for app creation:

flutter create fall_detection_app
cd fall_detection_app

Add dependencies

In pubspec.yaml:

dependencies:
  http: ^0.13.6
  sensors_plus: ^3.0.0

AFter this running command: flutter pub get

While testing:
http://192.168.X.X:8000/predict

In AndroidManifest.xml:

<application
    android:usesCleartextTraffic="true"
